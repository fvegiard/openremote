# Public port for Caddy on the host (use 80 on a VPS if you want plain HTTP behind Cloudflare)
PUBLIC_HTTP_PORT=8080

# Basic auth for OpenCode + /term/*
BASIC_AUTH_USER=dev
BASIC_AUTH_HASH=$2a$14$REPLACE_WITH_CADDY_HASH

# Optional stronger auth for /dockge/*
ADMIN_AUTH_USER=admin
ADMIN_AUTH_HASH=$2a$14$REPLACE_WITH_CADDY_HASH

# Optional Cloudflare Tunnel token (recommended approach)
CF_TUNNEL_TOKEN=REPLACE_ME

# GitHub Repository to clone
GITHUB_REPO_URL=
# GitHub Token (optional, for private repos)
GITHUB_TOKEN=